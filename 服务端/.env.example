# 服务配置
SERVER_PORT=8080

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=cursor_auth

# 上车配置
SAME_TLS_IP_ALLOW_MINUTES=10      # 相同TLS和IP允许再次上车的分钟数
MAX_ACCOUNT_USAGE=200             # 每个账号允许最大上车次数 